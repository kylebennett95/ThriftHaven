import { useNavigate } from "react-router"
import { ListingCard } from "./ListingCard"

export const Home = () => {

    const navigate = useNavigate()



    return (
        <ListingCard /> 
    )
}